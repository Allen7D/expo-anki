# Anki 单词闪卡 App 技术方案

## 1. 项目概述

基于现有的 Expo React Native + SQLite + Drizzle ORM 技术栈，开发一个功能完整的 Anki 风格单词闪卡复习应用。

## 2. 技术栈

### 核心技术
- **框架**: Expo React Native (v52)
- **语言**: TypeScript
- **数据库**: SQLite + Drizzle ORM
- **UI**: React Native 原生组件 + 自定义样式

### 主要依赖
```json
{
  "expo": "^52.0.47",
  "expo-sqlite": "~15.1.4", 
  "drizzle-orm": "^0.44.2",
  "react-native": "0.76.9",
  "@expo/vector-icons": "最新版本",
  "react-native-reanimated": "最新版本"
}
```

## 3. 数据库设计

### 3.1 现有表结构（保留）
```typescript
// users_table - 用户表
{
  id: int (主键，自增)
  name: text (非空)
  age: int (非空) 
  email: text (非空，唯一)
}
```

### 3.2 新增表结构

```typescript
// flashcards_table - 闪卡表
{
  id: int (主键，自增)
  word: text (非空) - 单词
  usphone: text - 美式音标
  ukphone: text - 英式音标
  meaning: text (非空) - 中文释义
  example: text - 例句
  imageUrl: text - 图片URL
  createdAt: text - 创建时间
  updatedAt: text - 更新时间
}

// user_flashcard_progress - 用户学习进度表
{
  id: int (主键，自增)
  userId: int (外键，关联用户)
  flashcardId: int (外键，关联闪卡)
  difficulty: int (难度等级：1-简单，2-一般，3-困难)
  nextReviewDate: text (下次复习时间)
  reviewCount: int (复习次数)
  correctCount: int (正确次数)
  lastReviewedAt: text (最后复习时间)
  createdAt: text (创建时间)
}

// study_sessions - 学习记录表
{
  id: int (主键，自增)
  userId: int (外键，关联用户)
  flashcardId: int (外键，关联闪卡)
  isCorrect: boolean (是否答对)
  responseTime: int (响应时间，毫秒)
  studiedAt: text (学习时间)
}
```

## 4. 核心功能模块

### 4.1 数据初始化模块
- 从 `flashcard.json` 导入初始单词数据
- 创建默认用户
- 初始化用户学习进度

### 4.2 闪卡学习模块
- **正面显示**: 英文单词 + 音标
- **背面显示**: 中文释义 + 例句 + 图片
- **翻卡动画**: 点击卡片或翻转按钮
- **难度评估**: 简单/一般/困难 三个按钮

### 4.3 间隔重复算法（简化版）
```typescript
// 基于 Anki 的 SM-2 算法简化版
function calculateNextReview(difficulty: number, reviewCount: number) {
  const intervals = {
    1: [1, 3, 7],      // 简单：1天、3天、7天
    2: [1, 2, 5],      // 一般：1天、2天、5天  
    3: [1, 1, 3]       // 困难：1天、1天、3天
  };
  
  const difficultyIntervals = intervals[difficulty] || intervals[2];
  const index = Math.min(reviewCount, difficultyIntervals.length - 1);
  return difficultyIntervals[index];
}
```

### 4.4 学习统计模块
- 今日学习数量
- 总学习时间
- 正确率统计
- 学习进度图表

### 4.5 复习队列管理
- 新卡片（未学习过的）
- 复习卡片（到期需要复习的）
- 学习完成状态

## 5. 应用界面设计

### 5.1 主要页面结构
```
App
├── HomeScreen (首页)
│   ├── 今日统计
│   ├── 开始学习按钮
│   └── 学习进度
├── StudyScreen (学习页面)
│   ├── 进度条
│   ├── 闪卡组件
│   └── 难度选择按钮
├── StatsScreen (统计页面)
│   ├── 学习数据
│   └── 进度图表
└── SettingsScreen (设置页面)
    ├── 用户信息
    └── 学习偏好
```

### 5.2 核心组件设计

```typescript
// FlashCard 组件
interface FlashCardProps {
  word: string;
  usphone: string;
  ukphone: string;
  meaning: string;
  example: string;
  imageUrl: string;
  isFlipped: boolean;
  onFlip: () => void;
}

// DifficultySelector 组件
interface DifficultySelectorProps {
  onSelect: (difficulty: 1 | 2 | 3) => void;
}

// ProgressBar 组件
interface ProgressBarProps {
  current: number;
  total: number;
}
```

## 6. 状态管理

使用 React Native 的内置状态管理：
- `useState` - 组件局部状态
- `useEffect` - 副作用处理
- `useContext` - 全局用户状态（如需要）

## 7. 目录结构

```
expo-anki/
├── db/
│   └── schema.ts (扩展数据库表)
├── src/
│   ├── components/
│   │   ├── FlashCard.tsx
│   │   ├── DifficultySelector.tsx
│   │   ├── ProgressBar.tsx
│   │   └── StatCard.tsx
│   ├── screens/
│   │   ├── HomeScreen.tsx
│   │   ├── StudyScreen.tsx
│   │   ├── StatsScreen.tsx
│   │   └── SettingsScreen.tsx
│   ├── services/
│   │   ├── database.ts
│   │   ├── studyAlgorithm.ts
│   │   └── dataImport.ts
│   ├── hooks/
│   │   ├── useFlashcards.ts
│   │   ├── useStudySession.ts
│   │   └── useStats.ts
│   ├── types/
│   │   └── index.ts
│   └── utils/
│       ├── dateHelpers.ts
│       └── constants.ts
├── drizzle/
├── assets/
├── App.tsx (主导航)
└── flashcard.json
```

## 8. 实现计划

### 阶段一：基础架构
1. 扩展数据库 schema
2. 数据迁移和初始化
3. 基础组件开发

### 阶段二：核心功能
1. 闪卡学习界面
2. 间隔重复算法
3. 学习进度追踪

### 阶段三：完善体验
1. 统计页面
2. 动画效果
3. 用户界面优化

### 阶段四：测试优化
1. 功能测试
2. 性能优化
3. 用户体验调试

## 9. 核心算法逻辑

### 学习流程
1. 获取今日待学习/复习的卡片
2. 按优先级排序（新卡片 → 复习卡片）
3. 显示卡片正面
4. 用户查看背面
5. 评估难度
6. 更新学习进度和下次复习时间
7. 记录学习会话数据

### 数据持久化
- 所有学习进度实时保存到 SQLite
- 支持应用重启后恢复学习状态
- 定期清理过期的学习记录

## 10. 性能考虑

- 使用 SQLite 索引优化查询性能
- 图片懒加载和缓存
- 分页加载大量卡片数据
- 动画使用 `react-native-reanimated` 确保 60fps

---

这个技术方案提供了一个完整、可实现的 Anki 风格单词闪卡应用框架，保留了原有的用户表结构，并充分利用了现有的技术栈。 