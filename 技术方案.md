# Anki 单词闪卡 App 技术方案

## 1. 项目概述

基于现有的 Expo React Native + SQLite + Drizzle ORM 技术栈，开发一个功能完整的 Anki 风格单词闪卡复习应用。

## 2. 技术栈

### 核心技术
- **框架**: Expo React Native (v52)
- **语言**: TypeScript
- **数据库**: SQLite + Drizzle ORM
- **UI**: React Native 原生组件 + 自定义样式

### 主要依赖
```json
{
  "expo": "^52.0.47",
  "expo-sqlite": "~15.1.4", 
  "drizzle-orm": "^0.44.2",
  "react-native": "0.76.9",
  "@expo/vector-icons": "最新版本",
  "react-native-reanimated": "最新版本"
}
```

## 3. 数据库设计

### 3.1 现有表结构（保留）
```typescript
// users_table - 用户表
{
  id: int (主键，自增)
  name: text (非空)
  age: int (非空) 
  email: text (非空，唯一)
}
```

### 3.2 新增表结构

```typescript
// flashcards_table - 闪卡表
{
  id: int (主键，自增)
  word: text (非空) - 单词
  usphone: text - 美式音标
  ukphone: text - 英式音标
  meaning: text (非空) - 中文释义
  example: text - 例句
  imageUrl: text - 图片URL
  createdAt: text - 创建时间
  updatedAt: text - 更新时间
}

// user_flashcard_progress - 用户学习进度表
{
  id: int (主键，自增)
  userId: int (外键，关联用户)
  flashcardId: int (外键，关联闪卡)
  difficulty: int (难度等级：1-简单，2-一般，3-困难)
  nextReviewDate: text (下次复习时间)
  reviewCount: int (复习次数)
  correctCount: int (正确次数)
  lastReviewedAt: text (最后复习时间)
  createdAt: text (创建时间)
}

// study_sessions - 学习记录表
{
  id: int (主键，自增)
  userId: int (外键，关联用户)
  flashcardId: int (外键，关联闪卡)
  isCorrect: boolean (是否答对)
  responseTime: int (响应时间，毫秒)
  studiedAt: text (学习时间)
}

// user_algorithm_config - 用户算法配置表
{
  id: int (主键，自增)
  userId: int (外键，关联用户，唯一)
  algorithmType: text (算法类型：anki/supermemo/custom)
  learningMode: text (学习模式：standard/fast/conservative/custom)
  baseIntervalsEasy: text (简单难度间隔，JSON格式)
  baseIntervalsNormal: text (一般难度间隔，JSON格式)
  baseIntervalsHard: text (困难难度间隔，JSON格式)
  multiplierEasy: real (简单难度倍增系数)
  multiplierNormal: real (一般难度倍增系数)
  multiplierHard: real (困难难度倍增系数)
  maxInterval: int (最大间隔天数)
  minInterval: int (最小间隔天数)
  newCardSteps: text (新卡片学习步骤，JSON格式)
  lapseSteps: text (遗忘重学步骤，JSON格式)
  dailyNewCards: int (每日新卡片数量)
  dailyReviewCards: int (每日复习卡片数量)
  createdAt: text (创建时间)
  updatedAt: text (更新时间)
}
```

## 4. 核心功能模块

### 4.1 数据初始化模块
- 从 `flashcard.json` 导入初始单词数据
- 创建默认用户
- 初始化用户学习进度

### 4.2 闪卡学习模块
- **正面显示**: 英文单词 + 音标
- **背面显示**: 中文释义 + 例句 + 图片
- **翻卡动画**: 点击卡片或翻转按钮
- **难度评估**: 简单/一般/困难 三个按钮

### 4.3 间隔重复算法（可配置版）

#### 4.3.1 算法配置结构
```typescript
interface SpacedRepetitionConfig {
  algorithmType: 'anki' | 'supermemo' | 'custom';
  baseIntervals: {
    easy: number[];    // 简单难度的间隔天数
    normal: number[];  // 一般难度的间隔天数  
    hard: number[];    // 困难难度的间隔天数
  };
  multipliers: {
    easy: number;      // 简单难度的倍增系数
    normal: number;    // 一般难度的倍增系数
    hard: number;      // 困难难度的倍增系数
  };
  maxInterval: number; // 最大间隔天数
  minInterval: number; // 最小间隔天数
  newCardSteps: number[]; // 新卡片学习步骤（分钟）
  lapseSteps: number[];   // 遗忘卡片重学步骤（分钟）
}

// 默认配置（类似 Anki）
const DEFAULT_CONFIG: SpacedRepetitionConfig = {
  algorithmType: 'anki',
  baseIntervals: {
    easy: [1, 4, 10],
    normal: [1, 2, 6],
    hard: [1, 1, 4]
  },
  multipliers: {
    easy: 2.5,
    normal: 2.0,
    hard: 1.3
  },
  maxInterval: 365,  // 最长一年
  minInterval: 1,    // 最短一天
  newCardSteps: [1, 10, 1440], // 1分钟、10分钟、1天
  lapseSteps: [10, 1440]        // 10分钟、1天
};
```

#### 4.3.2 核心算法实现
```typescript
class SpacedRepetitionEngine {
  constructor(private config: SpacedRepetitionConfig) {}

  // 计算下次复习间隔
  calculateNextInterval(
    difficulty: 1 | 2 | 3, // 1=简单, 2=一般, 3=困难
    reviewCount: number,
    previousInterval: number,
    isCorrect: boolean
  ): number {
    if (!isCorrect) {
      return this.calculateLapseInterval();
    }

    switch (this.config.algorithmType) {
      case 'anki':
        return this.calculateAnkiInterval(difficulty, reviewCount, previousInterval);
      case 'supermemo':
        return this.calculateSuperMemoInterval(difficulty, reviewCount, previousInterval);
      case 'custom':
        return this.calculateCustomInterval(difficulty, reviewCount, previousInterval);
      default:
        return this.calculateAnkiInterval(difficulty, reviewCount, previousInterval);
    }
  }

  // Anki 算法
  private calculateAnkiInterval(
    difficulty: 1 | 2 | 3,
    reviewCount: number,
    previousInterval: number
  ): number {
    const difficultyKey = difficulty === 1 ? 'easy' : difficulty === 2 ? 'normal' : 'hard';
    const baseIntervals = this.config.baseIntervals[difficultyKey];
    const multiplier = this.config.multipliers[difficultyKey];

    let interval: number;

    if (reviewCount < baseIntervals.length) {
      // 使用预设间隔
      interval = baseIntervals[reviewCount];
    } else {
      // 使用倍增系数
      interval = Math.round(previousInterval * multiplier);
    }

    return this.clampInterval(interval);
  }

  // SuperMemo SM-2 算法
  private calculateSuperMemoInterval(
    difficulty: 1 | 2 | 3,
    reviewCount: number,
    previousInterval: number
  ): number {
    // 将难度转换为 SM-2 的质量评分 (0-5)
    const quality = difficulty === 1 ? 5 : difficulty === 2 ? 3 : 1;
    
    if (reviewCount === 0) {
      return quality < 3 ? 1 : 1;
    }
    if (reviewCount === 1) {
      return quality < 3 ? 1 : 6;
    }

    // EF (easiness factor) 计算
    let ef = 2.5; // 初始值
    ef = ef + (0.1 - (5 - quality) * (0.08 + (5 - quality) * 0.02));
    ef = Math.max(1.3, ef);

    const interval = Math.round(previousInterval * ef);
    return this.clampInterval(interval);
  }

  // 自定义算法
  private calculateCustomInterval(
    difficulty: 1 | 2 | 3,
    reviewCount: number,
    previousInterval: number
  ): number {
    // 用户可以完全自定义的算法逻辑
    const difficultyKey = difficulty === 1 ? 'easy' : difficulty === 2 ? 'normal' : 'hard';
    const baseIntervals = this.config.baseIntervals[difficultyKey];
    
    if (reviewCount < baseIntervals.length) {
      return baseIntervals[reviewCount];
    }
    
    const multiplier = this.config.multipliers[difficultyKey];
    const interval = Math.round(previousInterval * multiplier);
    return this.clampInterval(interval);
  }

  // 遗忘重学间隔
  private calculateLapseInterval(): number {
    return this.config.lapseSteps[0] / (24 * 60); // 转换为天数
  }

  // 限制间隔范围
  private clampInterval(interval: number): number {
    return Math.min(
      Math.max(interval, this.config.minInterval),
      this.config.maxInterval
    );
  }

  // 新卡片学习步骤
  getNewCardSteps(): number[] {
    return this.config.newCardSteps;
  }

  // 更新配置
  updateConfig(newConfig: Partial<SpacedRepetitionConfig>): void {
    this.config = { ...this.config, ...newConfig };
  }
}
```

#### 4.3.3 预设配置模板
```typescript
// 预设的学习模式配置
export const PRESET_CONFIGS = {
  // 标准模式（适合大多数用户）
  standard: {
    algorithmType: 'anki' as const,
    baseIntervals: { easy: [1, 4, 10], normal: [1, 2, 6], hard: [1, 1, 4] },
    multipliers: { easy: 2.5, normal: 2.0, hard: 1.3 },
    maxInterval: 365, minInterval: 1,
    newCardSteps: [1, 10, 1440], lapseSteps: [10, 1440]
  },

  // 快速模式（加速学习）
  fast: {
    algorithmType: 'anki' as const,
    baseIntervals: { easy: [1, 3, 7], normal: [1, 2, 4], hard: [1, 1, 2] },
    multipliers: { easy: 3.0, normal: 2.5, hard: 1.5 },
    maxInterval: 180, minInterval: 1,
    newCardSteps: [1, 5, 720], lapseSteps: [5, 720]
  },

  // 保守模式（记忆更牢固）
  conservative: {
    algorithmType: 'anki' as const,
    baseIntervals: { easy: [1, 5, 15], normal: [1, 3, 10], hard: [1, 2, 6] },
    multipliers: { easy: 2.0, normal: 1.8, hard: 1.2 },
    maxInterval: 730, minInterval: 1,
    newCardSteps: [1, 15, 2880], lapseSteps: [15, 2880]
  },

  // 自定义模式（用户完全控制）
  custom: {
    algorithmType: 'custom' as const,
    baseIntervals: { easy: [1, 4, 10], normal: [1, 2, 6], hard: [1, 1, 4] },
    multipliers: { easy: 2.5, normal: 2.0, hard: 1.3 },
    maxInterval: 365, minInterval: 1,
    newCardSteps: [1, 10, 1440], lapseSteps: [10, 1440]
  }
};
```

#### 4.3.4 配置管理接口
```typescript
interface AlgorithmSettingsProps {
  currentConfig: SpacedRepetitionConfig;
  onConfigChange: (config: SpacedRepetitionConfig) => void;
  presets: typeof PRESET_CONFIGS;
}

// 用户可在设置页面调整的参数
interface UserConfigurableSettings {
  learningMode: keyof typeof PRESET_CONFIGS;
  customIntervals: {
    easy: string;    // "1,4,10" 格式
    normal: string;  // "1,2,6" 格式
    hard: string;    // "1,1,4" 格式
  };
  multipliers: {
    easy: number;
    normal: number;
    hard: number;
  };
  maxInterval: number;
  dailyNewCards: number;    // 每日新卡片数量
  dailyReviewCards: number; // 每日复习卡片数量
}
```

### 4.4 学习统计模块
- 今日学习数量
- 总学习时间
- 正确率统计
- 学习进度图表

### 4.5 复习队列管理
- 新卡片（未学习过的）
- 复习卡片（到期需要复习的）
- 学习完成状态

## 5. 应用界面设计

### 5.1 主要页面结构
```
App
├── HomeScreen (首页)
│   ├── 今日统计
│   ├── 开始学习按钮
│   └── 学习进度
├── StudyScreen (学习页面)
│   ├── 进度条
│   ├── 闪卡组件
│   └── 难度选择按钮
├── StatsScreen (统计页面)
│   ├── 学习数据
│   └── 进度图表
└── SettingsScreen (设置页面)
    ├── 用户信息
    ├── 学习偏好
    ├── 算法配置
    │   ├── 学习模式选择
    │   ├── 自定义间隔设置
    │   ├── 倍增系数调整
    │   └── 每日卡片限制
    └── 数据统计设置
```

### 5.2 核心组件设计

```typescript
// FlashCard 组件
interface FlashCardProps {
  word: string;
  usphone: string;
  ukphone: string;
  meaning: string;
  example: string;
  imageUrl: string;
  isFlipped: boolean;
  onFlip: () => void;
}

// DifficultySelector 组件
interface DifficultySelectorProps {
  onSelect: (difficulty: 1 | 2 | 3) => void;
}

// ProgressBar 组件
interface ProgressBarProps {
  current: number;
  total: number;
}

// AlgorithmConfigPanel 组件
interface AlgorithmConfigPanelProps {
  config: SpacedRepetitionConfig;
  onConfigChange: (config: SpacedRepetitionConfig) => void;
  onPresetSelect: (preset: keyof typeof PRESET_CONFIGS) => void;
}

// IntervalEditor 组件
interface IntervalEditorProps {
  label: string;
  intervals: number[];
  onIntervalsChange: (intervals: number[]) => void;
  maxValues?: number[];
}

// MultiplierSlider 组件
interface MultiplierSliderProps {
  label: string;
  value: number;
  min: number;
  max: number;
  step: number;
  onValueChange: (value: number) => void;
}
```

## 6. 状态管理

使用 React Native 的内置状态管理：
- `useState` - 组件局部状态
- `useEffect` - 副作用处理
- `useContext` - 全局用户状态（如需要）

## 7. 目录结构

```
expo-anki/
├── db/
│   └── schema.ts (扩展数据库表)
├── src/
│   ├── components/
│   │   ├── FlashCard.tsx
│   │   ├── DifficultySelector.tsx
│   │   ├── ProgressBar.tsx
│   │   ├── StatCard.tsx
│   │   ├── AlgorithmConfigPanel.tsx
│   │   ├── IntervalEditor.tsx
│   │   ├── MultiplierSlider.tsx
│   │   └── PresetSelector.tsx
│   ├── screens/
│   │   ├── HomeScreen.tsx
│   │   ├── StudyScreen.tsx
│   │   ├── StatsScreen.tsx
│   │   └── SettingsScreen.tsx
│   ├── services/
│   │   ├── database.ts
│   │   ├── studyAlgorithm.ts
│   │   ├── dataImport.ts
│   │   ├── algorithmConfig.ts
│   │   └── presetManager.ts
│   ├── hooks/
│   │   ├── useFlashcards.ts
│   │   ├── useStudySession.ts
│   │   ├── useStats.ts
│   │   ├── useAlgorithmConfig.ts
│   │   └── usePresets.ts
│   ├── types/
│   │   └── index.ts
│   └── utils/
│       ├── dateHelpers.ts
│       └── constants.ts
├── drizzle/
├── assets/
├── App.tsx (主导航)
└── flashcard.json
```

## 8. 实现计划

### 阶段一：基础架构
1. 扩展数据库 schema
2. 数据迁移和初始化
3. 基础组件开发

### 阶段二：核心功能
1. 闪卡学习界面
2. 间隔重复算法
3. 学习进度追踪

### 阶段三：完善体验
1. 统计页面
2. 动画效果
3. 用户界面优化

### 阶段四：测试优化
1. 功能测试
2. 性能优化
3. 用户体验调试

## 9. 核心算法逻辑

### 学习流程
1. 获取今日待学习/复习的卡片
2. 按优先级排序（新卡片 → 复习卡片）
3. 显示卡片正面
4. 用户查看背面
5. 评估难度
6. 更新学习进度和下次复习时间
7. 记录学习会话数据

### 数据持久化
- 所有学习进度实时保存到 SQLite
- 支持应用重启后恢复学习状态
- 定期清理过期的学习记录

## 10. 算法配置实现细节

### 10.1 配置持久化
```typescript
// 算法配置服务
class AlgorithmConfigService {
  async saveUserConfig(userId: number, config: SpacedRepetitionConfig): Promise<void> {
    await db.insert(userAlgorithmConfigTable).values({
      userId,
      algorithmType: config.algorithmType,
      learningMode: this.getPresetName(config),
      baseIntervalsEasy: JSON.stringify(config.baseIntervals.easy),
      baseIntervalsNormal: JSON.stringify(config.baseIntervals.normal),
      baseIntervalsHard: JSON.stringify(config.baseIntervals.hard),
      multiplierEasy: config.multipliers.easy,
      multiplierNormal: config.multipliers.normal,
      multiplierHard: config.multipliers.hard,
      maxInterval: config.maxInterval,
      minInterval: config.minInterval,
      newCardSteps: JSON.stringify(config.newCardSteps),
      lapseSteps: JSON.stringify(config.lapseSteps),
      dailyNewCards: config.dailyNewCards,
      dailyReviewCards: config.dailyReviewCards
    }).onConflictDoUpdate({
      target: userAlgorithmConfigTable.userId,
      set: { /* 更新字段 */ }
    });
  }

  async getUserConfig(userId: number): Promise<SpacedRepetitionConfig> {
    const userConfig = await db.select()
      .from(userAlgorithmConfigTable)
      .where(eq(userAlgorithmConfigTable.userId, userId))
      .limit(1);

    if (userConfig.length === 0) {
      return DEFAULT_CONFIG;
    }

    return this.deserializeConfig(userConfig[0]);
  }
}
```

### 10.2 设置界面实现
```typescript
// 设置页面中的算法配置部分
const AlgorithmSettingsSection: React.FC = () => {
  const [config, setConfig] = useState<SpacedRepetitionConfig>(DEFAULT_CONFIG);
  const [selectedPreset, setSelectedPreset] = useState<string>('standard');
  
  return (
    <View style={styles.section}>
      <Text style={styles.sectionTitle}>间隔重复算法设置</Text>
      
      {/* 预设模式选择 */}
      <PresetSelector
        presets={PRESET_CONFIGS}
        selectedPreset={selectedPreset}
        onPresetSelect={(preset) => {
          setSelectedPreset(preset);
          setConfig(PRESET_CONFIGS[preset]);
        }}
      />
      
      {/* 自定义间隔设置 */}
      {selectedPreset === 'custom' && (
        <View>
          <IntervalEditor
            label="简单难度间隔"
            intervals={config.baseIntervals.easy}
            onIntervalsChange={(intervals) => 
              setConfig(prev => ({
                ...prev,
                baseIntervals: { ...prev.baseIntervals, easy: intervals }
              }))
            }
          />
          <MultiplierSlider
            label="简单难度倍增系数"
            value={config.multipliers.easy}
            min={1.0}
            max={5.0}
            step={0.1}
            onValueChange={(value) =>
              setConfig(prev => ({
                ...prev,
                multipliers: { ...prev.multipliers, easy: value }
              }))
            }
          />
        </View>
      )}
    </View>
  );
};
```

### 10.3 算法验证和预览
```typescript
// 算法效果预览功能
interface AlgorithmPreviewProps {
  config: SpacedRepetitionConfig;
}

const AlgorithmPreview: React.FC<AlgorithmPreviewProps> = ({ config }) => {
  const engine = new SpacedRepetitionEngine(config);
  
  const previewData = useMemo(() => {
    const scenarios = [
      { difficulty: 1, reviewCount: 0, previousInterval: 0 },
      { difficulty: 2, reviewCount: 1, previousInterval: 1 },
      { difficulty: 3, reviewCount: 2, previousInterval: 2 },
    ];
    
    return scenarios.map(scenario => ({
      ...scenario,
      nextInterval: engine.calculateNextInterval(
        scenario.difficulty as 1 | 2 | 3,
        scenario.reviewCount,
        scenario.previousInterval,
        true
      )
    }));
  }, [config]);

  return (
    <View style={styles.previewContainer}>
      <Text style={styles.previewTitle}>算法效果预览</Text>
      {previewData.map((item, index) => (
        <Text key={index}>
          难度{item.difficulty} 第{item.reviewCount}次复习 → {item.nextInterval}天后
        </Text>
      ))}
    </View>
  );
};
```

## 11. 性能考虑

- 使用 SQLite 索引优化查询性能
- 图片懒加载和缓存
- 分页加载大量卡片数据
- 动画使用 `react-native-reanimated` 确保 60fps
- 算法配置缓存机制，避免频繁数据库访问
- 预设配置预加载，提升设置页面响应速度

## 12. 用户体验优化

### 12.1 算法学习曲线
- 提供学习向导，介绍不同算法模式的特点
- 智能推荐算法配置（基于用户学习表现）
- 配置变更影响预览，让用户了解调整后果

### 12.2 个性化建议
- 根据用户答题正确率建议调整难度系数
- 基于学习时间建议每日卡片数量
- 学习效果分析，推荐最适合的算法模式

---

这个技术方案提供了一个完整、可实现的 Anki 风格单词闪卡应用框架，特别强化了间隔重复算法的可配置性和个性化能力，保留了原有的用户表结构，并充分利用了现有的技术栈。用户可以根据自己的学习习惯和记忆特点，灵活调整算法参数，实现真正个性化的学习体验。 